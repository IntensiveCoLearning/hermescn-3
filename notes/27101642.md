- GitHub ID：27101642
- Name：CHS1997
- 时区：UTC+8
- 报名动机：研究一下如何使用ai
- 行业：未填写

## Notes

<!-- Content_START -->
<!-- DAILY_CHECKIN_2026-09-04_START -->
# 2026-09-04

# **Hermes Agent 使用指南（越用越聪明的 Agent）**

> 生成时间：2026-09-01\
> 这份指南基于 **Hermes Agent v0.20.6**（Nous Research 出品）的已安装源码（`Lib\site-packages\`）和你本机 `~/.hermes/` 目录的真实状态写成。\
> ✅ = 从源码/本机文件核实 ✅；❓ = 推测/未核实 ❓（见文末"可信度说明"）。

***

## **0. 一句话定位**

**Hermes 是一个"会学习的终端 Agent"**：模型可以换成便宜的（你本机就配的是 `deepseek-chat`），然后用三样东西补偿模型的智商——

1. **记忆**：记住关于你的事实，跨会话
2. **技能**：把做过的活变成可复用的 skill，越用越熟
3. **每轮后台自省**：每次对话结束，一个后台分身自动回顾"这轮值不值得记住/升级技能"

核心卖点：**你用得越多，它越懂你的工作方式** —— 这是它和 Claude Code 最本质的区别。

<br />

## **1. 你的 Hermes 现状（本机实测）**

| **项**        | **值**                          | **说明**                                                                                        |
| :----------- | :----------------------------- | :-------------------------------------------------------------------------------------------- |
| 版本           | 0.20.6                         | pip 包 `hermes-agent`，Nous Research 出品                                                         |
| 模型           | `deepseek/deepseek-chat`       | `~/.hermes/config.yaml`                                                                       |
| 人格文件         | `~/.hermes/SOUL.md`            | **默认的**，还没定制                                                                                  |
| 用户画像         | `~/.hermes/memories/USER.md`   | 已有 2 条：中文用户、办公场景                                                                              |
| 记忆文件         | `~/.hermes/memories/MEMORY.md` | 记忆库（跨会话事实）                                                                                    |
| 技能库          | `~/.hermes/skills/`            | 30 个，几乎全是 `lark-*` + `academic-search`/`devops`/`experiment-code`（从 claude-office-skills 拷来的） |
| 会话历史         | 11 个 session                   | `~/.hermes/sessions/`                                                                         |
| Hooks / Cron | 空                              | 都没用上                                                                                          |
| CLI 入口       | ⚠️ **当前是坏的**                   | `hermes` 命令报 `ModuleNotFoundError: No module named 'hermes_cli.main'`（见第 8 节修复）               |

**你的配置定位**：低成本模型 + 现有技能大半是"别人写的办公 skill"。真正让 Hermes 值回票价的做法，是让它从**你自己的使用**里长出专属技能 —— 这需要你用 `/learn`，后面讲。

<br />

## **2. Hermes vs Claude Code（都在你这台机器上，怎么分工）**

| **维度**    | **Hermes Agent**                           | **Claude Code**                      |
| :-------- | :----------------------------------------- | :----------------------------------- |
| 定位        | 会自学的通用 Agent                               | 工程 agent + 大量 MCP/技能                 |
| 模型        | 可换，支持 DeepSeek/Nous/OpenRouter/Z.ai/Kimi 等 | 固定 Claude 家族                         |
| 记忆        | **强**：USER.md + MEMORY.md + 后台自动学习         | 弱：靠 CLAUDE.md + 少量 memory            |
| 技能学习      | **强**：`/learn` 把经历变成 skill                 | 中：skill 主要靠装                         |
| 自我改进      | **每轮后台 review fork**                       | 无对应机制                                |
| 你的交易/行情工具 | 无                                          | **满配**（Gate/OKX/Bitget/TradingView…） |
| 你的飞书工具    | 已装全套 lark-\* skill                         | 也有 lark-\* skill                     |

**分工建议**：

* **Hermes 当"长期学习助理"**：办公文档处理、数据清洗、资料总结这类**重复性**工作，喂给它 → 它会沉淀成你的专属技能
* **Claude Code 当"重火力工程师"**：写代码、交易、链上分析、并行大任务

<br />

## **3. 核心机制拆解：它到底怎么"越用越聪明"**

### **3.1 每轮对话结束后的"自省分身"（最关键）✅**

源码 `agent/background_review.py` 写得很清楚：

```
```

```
你发消息 → 主 Agent 回答 → 主 Agent 悄悄启动一个后台守护线程 fork
   ↓
fork 重放这轮对话的快照，问自己一个问题：
  "这一轮对话，有没有值得 ①存进记忆 或 ②改进技能 的东西？"
   ↓
有 → 直接写入 memories/ 或 skills/（受审批门约束，见 3.4）
```

几个关键设计（都是源码确认的）：

* **fork 继承主 Agent 的全部运行时**（同一个模型、同一套密钥、同一份缓存系统提示）→ 命中前缀缓存，成本很低
* **fork 的工具白名单只有记忆 + 技能管理**，其他工具全部运行时拒绝 ✅ → 它**只负责学习，永远碰不到你的执行操作**，这是安全设计
* 每轮最多 fork 一次，输入预算默认上限 60 万 token（`max_input_tokens`），防止失控
* 主开关：`background_review.enabled`（默认 **true**）

**给你的实用建议**：如果你想省钱，可以把 review fork 路由到更便宜的模型（`background_review.provider/model`），官方注释说质量基本不降 ✅。

<br />

打开后用法：`/memory pending` 看待审批 → `/memory approve <id>` / `/memory reject <id>`。技能侧对应 `/skills pending`、`/skills approve`、`/skills diff <id>`、`/skills reject`。

**给你的建议**：先开着审批门用一段时间，看看它都学什么，再决定是否全自动。

### **3.5 手动触发自省：`/refine` ✅**

不想等每轮自动 fork？聊完一长段后敲 `/refine`，立即对当前会话跑一次记忆/技能 review，可带焦点：`/refine 记住用户用中文名交流`。

### **3.6 Journey 图谱：把你的"成长"可视化 ✅**

* CLI：`hermes journey`（时间线柱状图）
* 对话内：`/journey`（TUI overlay）
* 桌面版：Star Map / 记忆图谱

图谱节点 = 你学会的技能 + 记忆条目，按时间排列。技能间靠 `related_skills` 字段连边，记忆和技能靠词面重叠连边。可以 `hermes journey delete|edit` 手动修正它学错的东西。✅

***

## **4. 命令速查**

### **4.1 CLI 子命令（`hermes <subcommand>`）✅**

| **命令**                                         | **干什么**                         |
| :--------------------------------------------- | :------------------------------ |
| `hermes`                                       | 进入对话（主入口）                       |
| `hermes console`                               | 安全 console（有限命令集）               |
| `hermes cron`                                  | 定时任务（cron 目录）                   |
| `hermes gateway` / `dashboard` / `gui`         | 网关 / 网页版 / 桌面版                  |
| `hermes skills`                                | 技能管理（列表/装/卸/审核）                 |
| `hermes memory`                                | 记忆管理（status/off/reset）          |
| `hermes journey`                               | 学习图谱时间线                         |
| `hermes hooks`                                 | 事件钩子管理                          |
| `hermes import_agent`                          | 从其他 agent（Claude/Codex 等）导入技能 ✅ |
| `hermes doctor` / `debug` / `logs`             | 体检 / 调试 / 日志                    |
| `hermes auth` / `login` / `logout` / `pairing` | 登录 / 设备配对                       |
| `hermes model`                                 | 换模型                             |
| `hermes backup` / `sync` / `update` / `verify` | 备份 / 同步 / 升级 / 校验               |
| `hermes setup` / `config` / `profile` / `skin` | 配置 / 主题                         |
| `hermes plugins` / `mcp` / `tools`             | 插件 / MCP / 工具管理                 |
| `hermes security` / `security_audit`           | 安全检查                            |
| `hermes whatsapp` / `slack`                    | 接入消息平台                          |

### **4.2 对话内斜杠命令（在 Hermes 会话里敲）✅**

| **命令**                            | **干什么**                                |
| :-------------------------------- | :------------------------------------- |
| `/learn [来源]`                     | **把经历/资料变成技能（灵魂命令）**                   |
| `/refine [焦点]`                    | 手动触发一次记忆/技能学习                          |
| `/review`                         | 派独立子 agent 审查最近对话产出（PR/代码/文档），结论回传主会话  |
| `/memory`                         | 待审批记忆 + 审批门开关                          |
| `/skills`                         | 技能中心：pending/approve/reject/diff/mode  |
| `/journey`                        | 学习图谱                                   |
| `/init [目录]`                      | 扫描项目生成 AGENTS.md                       |
| `/blueprint [名字]`                 | 自动化蓝图（cron 任务对话化）                      |
| `/kanban`                         | 任务看板（拆解/批量执行）                          |
| `/heartbeat [pause/resume/clear]` | 空闲时定时触发任务（只活在当前进程，持久调度用 `hermes cron`） |
| `/billing` `/topup`               | 计费                                     |
| `/browser`                        | 浏览器控制                                  |
| `/stop`                           | 停止                                     |

<br />

## **工作流推荐：怎么让它"越用越聪明"**

### **三周养成法**

**第 1 周：喂模式**

* 把所有**你会重复做 2 次以上**的活都丢给它做（文档转格式、批量重命名、表格清洗…）
* 聊完一段就 `/refine`，让学习立即发生
* 打开审批门，每天 `/memory pending` + `/skills pending` 看它学了什么，`reject` 学错的

**第 2 周：固化**

* 看到它反复做对一件事，敲 `/learn`（无参 = 从当前对话学），把它固化成正式技能
* 用 `/skills diff <id>` 检查它写的技能对不对（`author` 是不是 Hermes？`description` 有没有超 60 字符？）
* `hermes journey` 每周看一次你的"技能增长曲线"

**第 3 周：调优**

* 把 `write_approval` 关掉，让它全自动学习（你已经信任它了）
* 把学歪的技能 `hermes journey delete` 删掉
* 技能多了以后用 `related_skills` 建立关联，图谱更清晰

### **和 Claude Code 的协作流**

```
```

```
Hermes（学习）                          Claude Code（执行）
────────────────────────────────────────────────────────
"每天把飞书会议纪要整理成周报"
   ↓ 重复 3 次后 /learn
飞书周报技能 ◄────── 技能目录互通 ──────► 直接复用
```
<!-- DAILY_CHECKIN_2026-09-04_END -->

<!-- DAILY_CHECKIN_2026-09-05_START -->
# 2026-09-05

Hermes 飞书机器人使用手册

把 AI 助手运行在腾讯云服务器上，7×24 常驻， 通过飞书随时对话。本手册覆盖日常使用、定时任务、联网能力的全部操作。

<br />

一、日常对话

在飞书里直接给机器人发消息即可，就像聊天一样：

<br />

帮我写一段 Python 代码：批量重命名文件

用一段话解释什么是 Kubernetes

把下面这段话翻译成英文：……

常用对话内命令（斜杠命令，直接发给机器人）：

<br />

命令	作用

/new 或 /reset	开启新对话（清空当前上下文）

/stop	中断正在进行的回答

/model	查看/切换模型

/status	查看机器人状态

/skills	浏览可用技能

/cron list	查看定时任务

/retry	重试上一轮回答

/undo	撤销上一轮

能力开关：

<br />

✅ 联网搜索/抓网页（Tavily）

✅ 浏览器自动化（能打开网页、点击、填表单）

✅ 记忆（跨对话记住你的偏好）

✅ 定时任务、文件读写、代码执行

❌ 视频/推特搜索/智能家居（未启用）

二、联网能力怎么用

搜索

直接说：

<br />

搜索一下最近的 AI 新闻

帮我查一下 xxx 是什么

抓取网页内容

抓取 https\://example.com 的内容，总结要点

浏览器自动化（需要交互时）

打开 https\://cn.bing.com 搜索"xxx"，告诉我前3条结果

打开这个网页，帮我填写表单……

搜索类问题 Hermes 会自动选最快的工具；需要登录/点击的页面才会动用浏览器。
<!-- DAILY_CHECKIN_2026-09-05_END -->
<!-- Content_END -->
