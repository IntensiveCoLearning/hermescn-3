- GitHub ID：3427329
- Name：joyc
- 时区：UTC+8
- 报名动机：打算从 OpenClaw 转到 Hermes 来继续养 Agent
- 行业：web3 &amp; AI

## Notes

<!-- Content_START -->
<!-- DAILY_CHECKIN_2026-09-03_START -->
# 2026-09-03

# 9/3 Day 1：安装并完成第一次对话（环境验证与 doctor 诊断）

### 1. Hermes Agent 是什么

 

Hermes 不只是聊天机器人或代码补全工具，而是能够**理解目标 → 调用工具 → 执行动作 → 观察结果 → 再决定下一步**的 Agent。

它可以操作文件、执行终端命令、访问网页、使用外部服务、保存 Memory / Skills，并通过 Gateway 接入消息平台。模型负责“判断”，工具负责“行动”。

一个简单判断标准：

> **固定规则、固定输入、固定输出 → 脚本更合适。**\
> **需要阅读、判断、多步骤操作和动态决策 → Agent 更有价值。**

适合 Hermes 的任务通常应该具备：

**目标 → 输入 → 权限范围 → 验收标准**

如果这四个东西说不清楚，不应该直接让 Agent 开始工作。

### 2. 安装与第一次运行

 

Hermes 有 **Desktop / CLI** 两种主要入口，本质上使用的是同一个 Agent 核心。我选择了 Desktop。\
![image.png](https://cdn.intensivecolearn.ing/images/programs/cmtfruwwx1ev1ro29dtsjp8v6/checkins/icl1-user-3427329/2026-09/6bf92165-d191-498f-843a-9119932a4c70.png)

### 3. 第一个真实任务：从“聊天”进入“执行”

 

第一个任务的重点不是生成一篇漂亮的 `summary.md`，而是第一次完整经历：

**读取输入 → 理解任务 → 使用工具 → 创建文件 → 检查结果 → 人工验收**

练习在独立的 `hermes-first-task` 目录中进行

一个好的 Agent Prompt 至少包含：

Input：允许读取哪些文件\
Goal：最终需要产生什么\
Structure：输出应该包含什么\
Constraints：哪些事情不能做\
Approval：哪些动作需要先询问\
Acceptance：怎样判断任务完成

例如：

> 只读取指定文件，不联网、不修改原文件，只创建 `summary.md`，完成后检查文件是否存在并核对内容。

这实际上是以后设计 Agent Workflow 最重要的基础模式。

### 4. Hermes 是如何工作的

 

可以把 Hermes 的核心执行模型理解成：

```
User Goal
   ↓
Context
   ↓
Model 判断下一步
   ↓
Tool 执行动作
   ↓
Tool 返回结果
   ↓
Model 再判断
   ↓
继续执行 / 请求人工确认 / 完成
```

这里有三个非常关键的认知：

**第一，模型 ≠ 工具。**\
&#x20;模型本身不能直接读取你的文件或修改电脑，它必须通过工具完成真实操作。

**第二，工具执行成功 ≠ 任务成功。**\
 例如命令返回 `exit code 0`，只代表命令执行成功，不代表业务结果正确；文件存在，也不代表内容正确。

**第三，Permission ≠ Validation。**\
&#x20;“允许 Agent 做这个动作”和“Agent 做出来的结果正确”是两件完全不同的事情。最终仍然需要验收。

因此一个可靠的 Agent Workflow 应该是：

> **Action → Observe → Validate**

而不是：

> **Action → Done**

### 5. 权限、安全与凭据

 

Hermes 的安全原则不是“一律禁止”，而是：

> **权限、操作范围和潜在后果必须匹配。**

每次批准一个动作，可以问自己五个问题：

1. 它会接触什么？
2. 它会改变什么？
3. 能不能恢复？
4. 结果会不会离开本机？
5. 是否涉及凭据、资金或生产系统？

风险大致从：

**读取指定文件 → 创建文件 → 覆盖文件 → 网络访问 → 删除 / 发布 / 生产环境 → 支付 / 转账 → 凭据泄露**

逐渐升高。

特别需要记住：

**API Key、OAuth Token、GitHub Token、云服务密钥、钱包私钥/助记词都属于凭据。**

不要把它们放进 Prompt、截图、Git、Issue 或聊天记录中。优先使用 OAuth 或 Hermes 的 credential management。
<!-- DAILY_CHECKIN_2026-09-03_END -->

<!-- DAILY_CHECKIN_2026-09-04_START -->
# 2026-09-04

# 9/4 Day2: Hermes的日常使用方法

#### 可执行的任务

6部分：\
\- 背景（context）\
\- 输入（inputs）\
\- 限制（constraints）\
\- 交付物（deliverable）\
\- 验收标准（acceptance criteria）\
概括为在什么背景下，基于哪些输入，在什么边界内，完成什么目标，留下什么交付物，并怎样证明它合格。

##### 管理会话、上下文与任务续接

标题解决查找，交接文件解决状态权威性，压缩解决上下文体积，新会话解决目标隔离；四者分工不同。

`hermes sessions list --source cli --limit 20`

会话太长或出现上下文限制时，先写交接摘要，再使用 `/compress`

#### 按任务选择最小工具集
<!-- DAILY_CHECKIN_2026-09-04_END -->

<!-- DAILY_CHECKIN_2026-09-05_START -->
# 2026-09-05

# 9/5 Day3: Hermes的日常使用方法2

### Memory、Skill 与会话信息

Memory 追求高信号和低体积；Skill 追求可执行方法；会话库保存真实历史；交接文件保存项目当前状态。

## 故障排查、更新与恢复

停止 → 状态 → 配置 → 凭据 → 日志 → 文档”的排错顺序\
善用Checkpoints为会话启用写入前快照，之后可以使用rollback来恢复。\
高风险任务仍应先备份、先草稿、先预览 diff，并由人确认具体执行。

## **接入飞书**
<!-- DAILY_CHECKIN_2026-09-05_END -->

<!-- DAILY_CHECKIN_2026-09-06_START -->
# 2026-09-06

# 9/6 Day4：**定时任务 和多媒体输入**

### **Cron 定时任务**

**核心概念**：Cron job 在每个 **fresh session（新会话）** 中运行，不记得创建时的上下文。因此 prompt 必须**自包含**——写清身份、行为边界和输出格式，不能出现"刚才那个东西"这类模糊指代。

**输出位置**：`$HOME/.hermes/cron/output/<job_id>/`

**关键认知**：

* 每次运行都调用模型 → 有 token 成本；忘记删除的重复任务会持续烧钱
* 交付到飞书前必须先验证 home channel 配置
* 故障排查顺序：`gateway status --deep` → `cron status` → `cron list --all` → 检查时区/凭据/模型
* 有固定输出的脚本类任务可考虑 **no-agent 模式**（免 LLM 成本）

### **语音、图片、文件输入**

**处理链路**：飞书上传 → Gateway 下载到本地缓存 → 工具/模型处理 → 回复

**隐私边界**（重要）：语音/图片/文档在**飞书服务端、Gateway 运行机器、云端 STT/Vision 服务**三处都可能形成副本。删除飞书消息 ≠ 清除所有缓存。决定"能不能发"前，先判断数据是否允许进入每一段链路。

**前置条件**：

* 飞书应用必须具备 `im:resource` 权限（访问用户发送的图片、文件、音频）
* 权限修改后需发布新版本 + 企业审批

**故障排查顺序**：

* 全部失败 → 检查 Gateway 状态 + 普通文字回复是否正常 + `im:resource` 权限
* 仅语音失败 → 检查音频格式/STT 依赖/首次模型下载
* 仅截图失败 → 确认附件已下载 + 裁剪放大重试
* 仅文件失败 → 先用 `.txt` 排除格式问题
* 回复发不回来 → 问题在交付段而非识别段

***

**总结**：Cron 的核心是"自包含 prompt + 成本意识 + 完整生命周期管理"；多媒体的核心是"逐项验证处理链 + 明确隐私边界 + 失败时代替猜测"。两个教程都强调：**先本地练熟，再接入真实外发。**
<!-- DAILY_CHECKIN_2026-09-06_END -->

<!-- DAILY_CHECKIN_2026-09-07_START -->
# 2026-09-07

# 9/7 Day5：扩展与协作

今天三个主题实际上解决的是一个问题：当单个 Hermes Agent 开始变复杂后，如何进行任务分工、身份隔离和能力扩展。可以把它们理解为三个层次：**Delegation 解决“谁来做” → Profiles 解决“以谁的身份做” → Tool / Skill / MCP / Plugin 解决“用什么能力做”。** 

#### 1. Delegation：把任务交给子代理

Delegation 的核心不是“多开几个 Agent”，而是把**相互独立、可以并行完成的子问题**交给 Subagent，然后由 Parent Agent 最终汇总和验收。

最重要的认知是：Subagent 没有父 Agent 的完整上下文。它只知道 Delegation 时传入的 `goal` 和 `context`，因此任务必须明确写出：目标、资料来源、限制条件、输出格式、截止范围以及失败处理方式。

适合 Delegation：

* 多个互不依赖的研究任务
* 大型代码 Review
* 不同角度的独立分析
* 可以并行执行的资料整理

不适合：

* 严格前后依赖的任务
* 简单的一次工具调用
* 确定性的脚本处理
* 需要不断向用户提问的任务

###### 最重要的原则

> **Subagent 负责执行，Parent Agent 负责判断和验收。**

不能因为两个 Subagent 得出了相同结论，就认为结论一定正确。最终仍然需要回到原始资料、文件、测试结果等真实证据进行验证。并发越高，模型成本、请求压力和潜在副作用也会同步增加。

***

#### 2. Profiles：隔离 Agent 的身份和状态

Profile 可以理解为 Hermes 的**独立 Agent 身份空间**。

一个 Profile 可以拥有独立的：

**配置 / Credentials / Sessions / Memory / Skills / Cron / Logs**

因此可以建立：

```
```

```
default
├── 日常个人 Agent

work
├── 工作项目 Agent

research
├── 技术研究 Agent

bot
└── 特定机器人 Agent
```

这样做的核心价值不是“多开几个窗口”，而是避免不同用途之间的**上下文、配置、凭据和长期状态互相污染**。

特别需要注意：

> **Profile ≠ Sandbox**

这是今天非常重要的一个知识点。

Profile 可以隔离 Hermes 自身的状态，但并不会自动限制操作系统层面的文件访问权限。`terminal.cwd` 只是指定工具默认从哪里开始工作，并不能阻止 Agent 访问其他目录；真正的强制访问边界需要 Sandbox 等机制。

另外，创建新 Profile 时不要随便使用 `--clone`。Clone 可能复制原 Profile 的 `.env`、配置和 Skills，从而把原来的 API Key 或机器人凭据带到新的身份中。

#### 3. Tool / Skill / MCP / Plugin：到底怎么扩展 Hermes？

这一部分可以建立一个非常实用的判断模型：

| 扩展方式       | 核心作用                     | 适合解决什么               |
| :--------- | :----------------------- | :------------------- |
| **Tool**   | 给 Agent 一个具体能力           | “让它能够做 X”            |
| **Skill**  | 给 Agent 一套知识 + 方法 + 工作流程 | “让它知道应该怎么做 X”        |
| **MCP**    | 接入外部工具 / 数据 / 服务         | “让它能够连接外部系统”         |
| **Plugin** | 更完整的扩展/集成机制              | “让 Hermes 获得一整套外部能力” |

最容易混淆的是 **Tool 和 Skill**：

**Tool 是能力，Skill 是方法。**

例如：

> Tool：可以执行 SQL。\
> &#x20;Skill：告诉 Agent 如何安全地分析 PostgreSQL、先查询再修改、如何验证结果。

所以，如果只是缺少一个动作能力，优先考虑 Tool；如果 Agent 已经有工具，但**不知道专业领域的正确工作方法**，Skill 更合适。

而 MCP 的价值在于把外部系统能力标准化地提供给 Agent，例如数据库、第三方 API、企业系统等。

***

#### 今天最重要的 6 个 Takeaways

**① Delegation 是“任务级并行”。**

**② Profile 是“身份级隔离”。**

**③ Tool 是“能力”，Skill 是“方法”。**

**④ MCP 是连接外部工具和数据系统的重要标准化接口。**

**⑤ Profile 隔离 Hermes 状态，但不等于操作系统 Sandbox。**

**⑥ Agent 越复杂，越应该把“执行”和“验收”分开。**
<!-- DAILY_CHECKIN_2026-09-07_END -->

<!-- DAILY_CHECKIN_2026-09-08_START -->
# 2026-09-08

# 9/8 Day6：案列学习

今天两个案例真正要学习的不是“怎么做一个研究摘要”或“怎么配置 Cron”，而是把前两天的 Agent 能力进一步变成一个可验证、可恢复、可自动运行、可控成本的 Agent Workflow。\
核心闭环可以浓缩成：\
**Input → Change Detection → Agent → Artifact → Automated Validation → Human Verification → Delivery**

意味着 Hermes 开始从“会执行任务的 Agent”进入“可以稳定运行的 Agent System”。

##### 1. 可验证研究：不要相信 Agent 的“完成”

第一个练习**整理一组可验证资料**最重要的思想是 **Acceptance ≠ Tool Success**。

Agent 成功创建 `research-summary.md`，并不意味着研究结果正确。案例通过 Fixture + Source Label + Hash + Verification Script + 人工核对 建立证据链：

```
固定输入
  ↓
Hash 验证输入没被修改
  ↓
Agent 整理资料
  ↓
每条事实标注 Source
  ↓
自动检查格式、章节、来源
  ↓
人工检查事实、冲突、未知状态
```

尤其值得记住的是：**冲突不能由 Agent 擅自“解决”**。例如两个来源给出不同预算数字，正确做法是保留冲突并标明来源，而不是让模型自行选择一个看起来更合理的数字。 

这实际上是研究型 Agent 最重要的原则：

> **Agent 可以整理信息，但不能偷偷改变信息的不确定性。**

***

##### 2. Source / State / Evidence：研究结果必须可追溯

今天进一步理解了一个很重要的概念：**研究产物不是普通文本，而应该是一种带 Evidence 的 Artifact。**

至少需要区分：

**已确认事实 / 建议 / 未确认 / 信息缺口 / 冲突**

不能把：

> “有人建议这样做”

自动变成：

> “已经决定这样做”。

同样，缺失的数据不能让 LLM 根据上下文“补全”。这就是为什么案例要求保留负责人、日期、数字、时区以及未知状态。 

因此以后设计 Research Skill 时，可以把：

**Source → Claim → Evidence → Confidence → Conflict**

作为基本数据结构思考，而不是单纯要求 Agent “总结一下”。

***

##### 3. Daily Research Assistant：从一次性任务进入持续运行

第二个练习进一步把上述方法变成每日自动化：

```
Cron
 ↓
Pre-check Gate
 ↓
有没有变化？
 ├─ No → 不调用模型
 └─ Yes
      ↓
   Fresh Agent Session
      ↓
   读取资料
      ↓
   生成 daily-brief.md
      ↓
   自动验收
      ↓
   Delivery → 飞书
```

这里最值得学习的是 **Pre-check Gate**。

如果每天资料没有变化，就根本不应该唤醒 LLM。这样可以同时降低：

**Token 成本 + 模型调用次数 + Agent 风险 + 无意义执行。**

案例明确要求 `wakeAgent: false` 时跳过 Agent，而不是把“没有变化”理解成任务失败。 

***

##### 4. Fresh Session：自动化 Agent 不能依赖聊天记忆

Cron 每次运行都是新的 Session，因此 Prompt 必须 **Self-contained**。

也就是说：**不能假设 Agent 记得昨天发生了什么。**

需要让任务本身包含：

**输入位置 + 任务目标 + 输出位置 + 判断规则 + 限制条件 + 验收要求。**

这和我们之前学习的 Profile / Delegation 其实是连起来的：**Agent System 越自动化，就越不能依赖隐式上下文。**

***

##### 5. 今天最重要的工程思想：Gate + Artifact + Acceptance

我认为今天最值得记住的是这三个概念：

###### Gate：要不要让 Agent 工作？

通过变化检测、条件判断、权限检查决定是否唤醒模型。

###### Artifact：Agent 到底产生了什么？

例如：

```
research-summary.md
daily-brief.md
verification result
execution log
```

Agent 的价值应该最终沉淀为**可检查的产物**，而不是停留在聊天窗口。

###### Acceptance：产物真的合格吗？

自动脚本检查结构、文件、Hash、来源等；人工检查语义、事实、冲突和判断。

所以完整闭环应该是：**Trigger ≠ Execution ≠ Success**

真正的 Success 是：**Trigger → Execution → Artifact → Validation → Delivery**
<!-- DAILY_CHECKIN_2026-09-08_END -->

<!-- DAILY_CHECKIN_2026-09-09_START -->
# 2026-09-09

# 9/9 Day7：个人项目展示

vibe出来个加密货币数据监控分析小助手

**Crypto Sentinel v3 — Hermes Agent 配置指南**

### 1. 项目概述

#### 1.1 Agent 名称与版本

* **名称**：Crypto Sentinel（加密货币市场哨兵）

* **版本**：v3

* **类型**：Hermes Skill（可注册为 Hermes Agent 的异步扫描模块）

##### 1.2 设计目标

构建一个 **多维度实时监控 + 交叉验证** 的加密货币市场异常检测系统，通过 12 条信号覆盖价格、成交量、链上行为、衍生品市场、情绪面五个维度，辅以 9 条交叉验证规则自动过滤噪声并识别高置信度交易信号。

#### 1.3 解决的问题

| 问题              | 解决方案                                              |
| :-------------- | :------------------------------------------------ |
| 免费信号噪音多、准确率低    | 交叉验证引擎（A 类同向确认 + B 类矛盾检测 + C 类链式验证）自动过滤假信号        |
| 多个监控工具分散，无法统一决策 | 12 条信号聚合为单一总分（加权求和），按总分触发告警级别                     |
| API Key 缺失时整体失效 | 按信号模块独立降级，Key 缺失仅跳过对应信号，不影响其他模块                   |
| 错过关键链上异动        | 支持 quick（5 分钟）/ full（15 分钟）/ daily（08:00 JST）三层调度 |

#### 1.4 使用场景

1. **日内短线交易者**：依赖 quick 模式监控价格与成交量异动，参考 full 模式的全维度信号
2. **中长线投资者**：每日 08:00 JST 接收情绪日报，关注链上基本面（MVRV、SOPR）趋势
3. **量化团队**：消费 JSONL 告警日志做回测与策略优化
4. **社群管理员**：通过 WeCom 接收 CRITICAL / WARNING 级别推送，及时在社群发布预警

### 2. 功能需求

##### 2.1 核心功能

###### 2.1.1 三层调度模式

| 模式      | 频率           | 覆盖信号                  | 交叉验证 | 耗时目标    | 典型应用                 |
| :------ | :----------- | :-------------------- | :--- | :------ | :------------------- |
| `quick` | 每 5 分钟       | 信号 1-5（价格 + 成交量 + 情绪） | 否    | < 30 s  | 轻量快速扫描，适合频密监控        |
| `full`  | 每 15 分钟      | 信号 1-12（全部）           | 是    | < 2 min | 全面扫描，默认模式            |
| `daily` | 每日 08:00 JST | 信号 4（情绪）+ 汇总          | 否    | < 1 min | 生成 Markdown 日报，不推送告警 |

###### 2.1.2 12 条信号清单

| #  | 信号名称         | 权重 | 数据源                     | 触发条件                              | API Key 需求                         |
| :- | :----------- | :- | :---------------------- | :-------------------------------- | :--------------------------------- |
| 1  | 价格 Z-score   | 30 | CoinGecko               | 最新收盘价偏离 1h 均值超过 2σ                | **免费**                             |
| 2  | 成交量突增        | 25 | CoinGecko               | 5min 成交量 > 1h 均量 × 3              | **免费**                             |
| 3  | 巨鲸转账         | 20 | Etherscan               | 单笔链上转账 ≥ $1,000,000（默认阈值）         | 需要 Etherscan API Key               |
| 4  | 情绪急变         | 15 | Alternative.me          | Fear & Greed Index 1h 变化 > 10 点   | **免费**                             |
| 5  | 相关性断裂        | 10 | CoinGecko               | BTC-ETH 30 日滚动 Pearson 相关系数 < 0.7 | **免费**                             |
| 6  | 交易所净流入       | 20 | Glassnode / CryptoQuant | 1h 净流入量 > 2σ 偏离均值                 | 需要 Glassnode 或 CryptoQuant API Key |
| 7  | 稳定币增发        | 15 | Etherscan               | 1h 内 USDT/USDC 增发 ≥ $50,000,000   | 需要 Etherscan API Key               |
| 8  | OI 异动        | 15 | Coinglass               | 1h 未平仓合约变化 > 20%                  | 需要 Coinglass API Key               |
| 9  | 资金费率极端       | 10 | Coinglass               | 费率 > 0.1%（多头极端）或 < -0.05%（空头极端）   | 需要 Coinglass API Key               |
| 10 | MVRV Z-score | 10 | Glassnode               | Z-score > 2.0（市场过热）或 < -2.0（市场低估） | 需要 Glassnode API Key               |
| 11 | SOPR         | 10 | Glassnode               | < 0.98（投降式抛售）或 > 1.05（获利了结）       | 需要 Glassnode API Key               |
| 12 | 清算量异动        | 10 | Coinglass               | 1h 清算量 > 日均 × 2                   | 需要 Coinglass API Key               |

> **总分上限**：12 条信号同时触发时权重之和为 190 分（但实战中极少全部触发）。

###### 2.1.3 交叉验证引擎

交叉验证在信号评分基础上自动修正总分和告警级别，分为 A / B / C 三类共 9 条规则。

##### A 类——同向确认（向量放大）

| 规则 ID | 名称     | 乘数因子 | 触发条件                                                                       | 效果             |
| :---- | :----- | :--- | :------------------------------------------------------------------------- | :------------- |
| A1    | 聪明钱建仓  | ×1.8 | 交易所净流出（信号6 raw\_value < 0）+ 稳定币增发（信号7 triggered）                           | 基础权重增量为 +0.8 倍 |
| A2    | 杠杆多头过热 | ×1.5 | OI 激增（信号8 triggered）+ 费率飙升多头（信号9 triggered, direction=long）+ 价格横盘（信号1 未触发） | 基础权重增量 +0.5 倍  |
| A3    | 投降式抛售  | ×1.6 | SOPR < 0.98（信号11 triggered, type=capitulation）+ F\&G < 25（信号4 extra < 25）  | 基础权重增量 +0.6 倍  |
| A4    | 大户出货准备 | ×1.5 | 交易所流入暴增（信号6 triggered, raw\_value > 0）+ 巨鲸转账（信号3 triggered）                | 基础权重增量 +0.5 倍  |

##### B 类——矛盾检测（强制覆盖级别）

| 规则 ID | 名称     | 覆盖效果                | 触发条件                                                                    | 说明                  |
| :---- | :----- | :------------------ | :---------------------------------------------------------------------- | :------------------ |
| B1    | 疑似拉高出货 | 🟥 **强制升 CRITICAL** | 价格上涨（信号1 z\_score > 0）+ 交易所流入暴增（信号6 raw\_value > 0）                     | 价格在涨但钱在流出交易所，典型拉高出货 |
| B2    | 聪明钱抄底  | ⬇️ **降为 WARNING**   | 价格下跌（信号1 z\_score < 0）+ 交易所流出（信号6 raw\_value < 0）+ 稳定币增发（信号7 triggered） | 价格下跌但聪明钱在买入，减轻恐慌评级  |
| B3    | 多空博弈前兆 | 🟨 **降为 NOTICE**    | 成交量暴增（信号2 triggered）+ 价格没动（信号1 未触发）                                     | 巨量换手但价格不动，多空等待突破    |

##### C 类——链式验证（固定加分）

| 规则 ID | 名称     | 加分      | 触发条件                                                             |
| :---- | :----- | :------ | :--------------------------------------------------------------- |
| C1    | 完整买入链路 | **+40** | 稳定币增发（信号7）→ 交易所流出（信号6 raw\_value < 0）→ 成交量突增（信号2），三者在 1h 窗口内全部触发 |
| C2    | 大资金开仓  | **+30** | 巨鲸转账（信号3）→ OI 激增（信号8）→ 价格异动（信号1），三者在 1h 窗口内全部触发                  |

##### 决策流程图（伪代码）

```
原始总分 = ∑(触发信号的权重)
        ↓
   A 类确认 → 总分 × 乘数因子
        ↓
   B 类矛盾 —→ 覆盖级别 (CRITICAL / WARNING / NOTICE)
        ↙         ↘
   C 类链式 → 固定加分 (+40 / +30)
        ↓
最终级别 = B类覆盖 ?? B类覆盖 : 阈值判定(最终总分)
```

###### 2.1.4 告警路由器

四个告警级别，以 **最终总分** 或 **B 类强制覆盖** 确定：

| 级别       | 总分阈值          | 推送行为                            | 符号 | 说明              |
| :------- | :------------ | :------------------------------ | :- | :-------------- |
| CRITICAL | ≥ 80 或 B1 强制  | WeCom Markdown 推送 + stdout JSON | 🚨 | 市场出现明确的高置信度异常信号 |
| WARNING  | 50–79 或 B2 强制 | WeCom Markdown 推送 + stdout JSON | ⚠️ | 多个信号指向同一方向，需关注  |
| NOTICE   | 30–49 或 B3 强制 | 仅本地日志 + JSONL                   | 🟨 | 单一信号触发，建议参考     |
| INFO     | < 30          | 仅本地日志                           | 🟩 | 正常状态，无显著异常      |

> **推送规则**：CRITICAL 和 WARNING 级别才通过 WeCom webhook 推送；NOTICE 和 INFO 仅记录本地日志和 JSONL 文件。

##### 告警消息格式示例

> ##### 🚨 Crypto Sentinel CRITICAL | BITCOIN
>
> 总分: 150.0 (基础: 120.0 | 修正: 30.0)
>
> 触发信号:
>
> > **#1 价格Z-score** (权重30)
> > 上涨偏离 Z-score=3.21, 当前价=$67,234.00, 均值=$62,180.00
> > **#3 巨鲸转账** (权重20)
> > 检测到 3 笔 ≥$1,000,000 转账，最大 $12,500,000
> > **#7 稳定币增发** (权重15)
> > 1h 预估增发=$120,000,000 (5 笔)
>
> 交叉验证:
>
> > \[A1] 聪明钱建仓 (×1.8)
> > \[C1] 完整买入链路 (+40)
>
> 摘要: \[BITCOIN] 触发3条信号: 价格Z-score+巨鲸转账+稳定币增发 | 验证: A1+C1
> 时间: 2026-09-09T22:08:00+09:00

#### Hermes Skill 注册

##### SKILL.md 配置

项目根目录的 `SKILL.md` 是 Hermes Skill 的说明书文件，包含：

* 技能概述

* 三种调度模式及其频率建议

* 告警推送说明（WeCom 推送逻辑 + JSON stdout 格式）

* 信号权重速查表

* 交叉验证标签速查表

* API Key 降级规则说明

##### 在 Hermes 中注册 Skill

1. 将 `crypto_sentinel/` 完整目录复制到 Hermes Agent 的 `skills/` 目录下
2. Hermes 会自动识别 `SKILL.md` 并注册为可用技能
3. 注册后可在 Agent 的 crontab 中引用该技能

#### Cron 调度配置

##### Hermes Cron 配置示例

```
# 在 Agent 的 crontab 中添加以下条目

# quick 模式：每 5 分钟（免费信号 1-5，无 API Key 需求也能运行）
- schedule: "*/5 * * * *"
  command: "cd /path/to/skills/crypto_sentinel && python crypto_sentinel.py --mode quick"
  description: "Crypto Sentinel quick scan (every 5 min)"

# full 模式：每 15 分钟（全部 12 条信号 + 交叉验证）
- schedule: "*/15 * * * *"
  command: "cd /path/to/skills/crypto_sentinel && python crypto_sentinel.py --mode full"
  description: "Crypto Sentinel full scan (every 15 min)"

# daily 模式：每日 08:00 JST (UTC+9) / 09:00 CST (UTC+8)
# 对应 UTC 23:00 (JST) 或 UTC 00:00 (CST)
- schedule: "0 23 * * *"
  command: "cd /path/to/skills/crypto_sentinel && python crypto_sentinel.py --mode daily"
  description: "Crypto Sentinel daily report (08:00 JST)"
```

##### 时区说明

| 地区       | Cron (UTC)   | 本地时间      |
| :------- | :----------- | :-------- |
| 日本 (JST) | `0 23 * * *` | 08:00 JST |
| 中国 (CST) | `0 0 * * *`  | 08:00 CST |

> Agent 时区建议设置为 `Asia/Tokyo`，日报 cron 可根据 Agent 所在时区调整。

**挂载 Skill**

确保 `crypto_sentinel/` 目录在 Hermes Agent 的 Skill 搜索路径下，`SKILL.md` 会被自动识别。

**配置 Cron**

在 Hermes Agent 的 crontab 中添加上文的三条 cron 配置（quick、full、daily）。
<!-- DAILY_CHECKIN_2026-09-09_END -->
<!-- Content_END -->
